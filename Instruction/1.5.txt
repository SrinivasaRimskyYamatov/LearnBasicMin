function 関数名(引数)    //関数　mainが実行する関数
    return 戻り
end

var 変数名 = 初期値    //変数宣言　型は無条件でdouble functionなどの中にあった場合無条件でローカル変数

変数=式    //代入分

print 変数名    //変数の中身を出力
print "文字列"    //文字列を出力

input 変数名    //数字入力

if(条件式)then    //条件分岐
else if(条件式)then
else
end

while(条件式)    //条件がtrueの間のみ実行
end

rand(最小値,最大値)    //範囲内の乱数を生成

load("ファイル名")    //分割されたプログラムファイルを読み込む

wait ミリ秒    //指定したミリ秒休む

//コメント    //一行コメント
#-    //複数行コメント
コメント
-#

window.new(戻りが入る予定の変数,縦サイズ,横サイズ)    //画面生成 複数生成できるが管理番号は変えなければ 戻り値は管理番号

window.delete(管理番号)    //画面削除

window.name(管理番号,"新しい名前")    //画面のタイトル変更

window.size(管理番号,縦サイズ,横サイズ)    //画面の大きさ変更

window.getkey(id, 変数名)    //押されたキーのキーコードを変数に代入

window.mousex(id, 変数名)    //マウスX座標を変数に代入

window.mousey(id, 変数名)    //マウスY座標を変数に代入

window.mousebutton(id, 変数名)    //マウスボタン状態(0=なし, 1=左, 2=右)を代入

setcolor(管理番号,r,g,b)    //rgbの設定

draw.put(管理番号,x,y)    //指定した座標に点を撃つ 色は現在のRGB

draw.line(管理番号,開始x,開始y,終了x,終了y)    //指定した管理番号の画面に線を引く 色は現在のRGB

draw.box(管理番号,開始x,開始y,終了x,終了y)    //指定した管理番号の画面に四角形を描画する(周りの線だけ) 色は現在のRGB

draw.circle(管理番号,中心x,中心y,半径)    //指定した管理番号の画面に円を描画する(周りの線だけ)色は現在のRGB

draw.fill(管理番号,x座標,y座標)    //指定した管理番号の画面に、指定した座標が線などで囲われている場合現在のRGBで塗りつぶす

draw.tri(管理番号,x1,y1,x2,y2,x3,y3)    //指定した管理番号の画面に、指定した頂点で三角形を描画する 色は現在のRGB

draw.cls(管理番号)    //指定した管理番号の画面の中身をすべて消す

draw.text(id, x, y, "文字列", 倍率)    //文字描画(倍率は省略可、デフォルト1.0)

draw.image(id, "ファイル名", x, y, x2, y2)    //画像描画(x2,y2省略可＝原寸)
